# Asegura que pec1 está al día en remoto

git checkout pec2
git status           
git push origin pec2

# Fusiona pec2 → main

git checkout main
git fetch origin
git pull --ff-only origin main
git merge --no-ff pec2 
# si hay conflictos: resuélvelos, luego:
# git add .
# git commit
git push origin main

# Sigue trabajando en pec2


git checkout pec2
git merge origin/main           